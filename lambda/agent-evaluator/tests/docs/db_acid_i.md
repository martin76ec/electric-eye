# Isolation (ACID)
Isolation is the 'I' in ACID. This property ensures that the concurrent execution of transactions results in a system state that would be obtained if transactions were executed sequentially (one after another). A transaction in process should not be affected by any other concurrent transactions.
